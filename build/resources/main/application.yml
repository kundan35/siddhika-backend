spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/siddhika
    username: siddhika
    password: siddhika
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080
